# nanoGPT dependencies
# Install with: pip install -r requirements.txt
#
# For CUDA 13.0 (RTX 4050, driver 580.88):
#   pip install torch --index-url https://download.pytorch.org/whl/cu130
# For CPU-only:
#   pip install torch

torch>=2.0
numpy>=1.24

# M6 扩展数据（可选）
# datasets>=2.0      # HuggingFace datasets，用于流式加载大型语料
# tqdm>=4.60          # 进度条
